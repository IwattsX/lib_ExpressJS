# Environment Variables

# MongoDB Database URL (you can get this from your MongoDB Atlas or local MongoDB setup)
MONGO_URI=mongodb://localhost:27017/mydatabase

# Secret key for JWT (keep this secret in production)
JWT_SECRET=mysecretkey

# Port where the server will run (can be overridden by a cloud provider)
PORT=5000

# Other optional configurations (such as API keys, or specific settings)
NODE_ENV=development

